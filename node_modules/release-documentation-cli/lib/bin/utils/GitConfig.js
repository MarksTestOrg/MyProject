'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _parseGitConfig = require('parse-git-config');

var _parseGitConfig2 = _interopRequireDefault(_parseGitConfig);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

class GitConfig {
  constructor(gitConfigPath) {
    this._config = _parseGitConfig2.default.sync({ path: gitConfigPath });
  }

  get url() {
    if (this._config && this._config['remote "origin"']) {
      return this._config['remote "origin"'].url;
    } else {
      return null;
    }
  }

  get repoDetails() {
    if (this.url) {
      return this.url.replace('git@github.com:', '').replace('.git', '');
    } else {
      return null;
    }
  }
}
exports.default = GitConfig;