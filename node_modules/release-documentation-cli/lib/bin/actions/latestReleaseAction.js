'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _gitInteractions = require('../github/gitInteractions');

exports.default = async (userDetails, repoDetails) => {
  try {
    const latestReleaseResponse = await (0, _gitInteractions.latestRelease)(userDetails, repoDetails);
    console.log(`The latest release version for ${repoDetails} is ${latestReleaseResponse.name}`);
  } catch (err) {
    console.error('Failed to get the latest release version', err);
    process.exitCode = 1;
  }
};